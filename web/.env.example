# REST Countries v5 API key (https://restcountries.com/sign-up)
# For browser calls, allowlist your hostname (e.g. "localhost") on the API Keys page.
NEXT_PUBLIC_RESTCOUNTRIES_API_KEY=your_api_key_here
